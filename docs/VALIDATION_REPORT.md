# VALIDATION_REPORT.md

## 1. Проверка структуры `raw_results.csv`

- Формат колонок совпадает со спецификацией в `docs/METRICS_SPEC.md` — OK
- Нет пропусков в обязательных полях — OK
- Все timestamp корректные (`submit <= start <= end`) — OK
- `task_duration_s >= 0` — OK
- `sched_overhead_ms >= 0` — OK
- `cpu_avg_pct` и `gpu_avg_pct` в диапазоне `[0, 100]` — OK
- `failed ∈ {0,1}` — OK

## 2. Проверка статистики

- Общее количество строк: __________
- Количество уникальных `run_id`: __________
- Полнота матрицы `(mode, n_tasks, repeat)` для сценария В: __________
- Количество failed задач: __________

## 3. Вывод

Данные готовы к агрегации и построению графиков.
